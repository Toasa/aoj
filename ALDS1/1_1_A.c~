#include <stdio.h>

static int *A;

void insertion_sort(int n) {
  for (int i = 1; i < n; i++) {
	int val = A[i];
	int j = i - 1;
	while (0 <= j && val < A[j]) {
	  A[j + 1] = A[j];
	  j--;
	}
	A[j + 1] = val;
  }
}

int main() {
  int n;
  scanf("%d", &n);
  A[n];
  for (int i = 0; i < n; i++)
	scanf("%d", A + i);

  insertion_sort(n);
  for (int i = 0; i < n; i++)
	printf("%d ", *(A + i));

  return 0;
}
